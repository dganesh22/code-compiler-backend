#include <stdio.h>
int main() {
    double a=1123.34, b=14.41, product=0;

    // Calculating product
    rem = a % b;

    // %.2lf displays number up to 2 decimal point
    printf("Product = %.2lf", product);
    
    return 0;
}