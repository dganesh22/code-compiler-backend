#include <stdio.h>
int main() {
    double a=85, b=96, product=96;
 
    // Calculating product
    product = a * b;

    // %.2lf displays number up to 2 decimal point
    printf("Product = %.2lf", product);
    
    return 0;
}