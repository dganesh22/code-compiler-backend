#include <stdio.h>
int main() {
    double a=20, b=24, product=0;

 
    // Calculating product
    product = a * b;

    // %.2lf displays number up to 2 decimal point
    printf("Product = %.2lf", product);
    
    return 0;
}