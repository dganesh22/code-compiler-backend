#include <stdio.h>
int main() {    

    int number1=45, number2=56, sum=0;
   

    // calculate the sum
    sum = number1 + number2;      
    
    printf("%d + %d = %d", number1, number2, sum);
    return 0;
}